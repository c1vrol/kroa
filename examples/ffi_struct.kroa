struct c CPoint:
    x: i64
    y: i64

extern "C" fn kroa_point_sum(p: CPoint) -> i64

fn main() -> i64:
    unsafe:
        let p = CPoint { x: 10, y: 32 }
        print_i64(kroa_point_sum(p))
    return 0
